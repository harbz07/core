{
  "name": "launch-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Launch-planning agent app built with current OpenAI Responses/Agents SDK patterns.",
  "scripts": {
    "dev": "node ./src/server/dev-server.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.js",
    "verify:stream": "node ./src/tests/verify-stream.mjs"
  },
  "dependencies": {
    "@openai/agents": "latest"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.5",
    "vitest": "^3.2.4"
  }
}
